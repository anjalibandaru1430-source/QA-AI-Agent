services:
  - type: web
    name: qagent-backend
    runtime: node
    plan: free
    buildCommand: npm install --prefix apps/server && npm run build --prefix apps/server
    startCommand: node apps/server/dist/app.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
